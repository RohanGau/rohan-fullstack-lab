{
  "name": "mcp-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "predev": "pnpm --filter @fullstack-lab/types build",
    "dev": "tsx src/index.ts",
    "prebuild": "pnpm --filter @fullstack-lab/types build",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "precheck": "pnpm --filter @fullstack-lab/types build",
    "check": "tsc --noEmit"
  },
  "dependencies": {
    "@fullstack-lab/constants": "workspace:*",
    "@fullstack-lab/types": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.17.5",
    "dotenv": "^17.2.0",
    "zod": "^4.0.15"
  },
  "devDependencies": {
    "@types/node": "^25.2.2",
    "tsx": "^4.20.3",
    "typescript": "^5.9.2"
  }
}
